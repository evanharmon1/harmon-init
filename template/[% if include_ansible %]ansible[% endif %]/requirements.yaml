---
# Ansible Galaxy collections.
# Install: uv run ansible-galaxy collection install -r ansible/requirements.yaml
# TODO: add/remove collections to match your roles.
collections:
  - name: community.docker
    version: ">=3.5.0"
  - name: community.general
    version: ">=8.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
