#!/usr/bin/env bash
set -euo pipefail

if [ ! -f package.json ] || [ ! -d dist ]; then
    echo "validate:web: app/build not set up yet -- skipping (scaffold the app and run the build)"
    exit 0
fi

task validate:jsonld
task validate:og
task validate:responsive
task validate:links
