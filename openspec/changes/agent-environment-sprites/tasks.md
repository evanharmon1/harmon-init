## 0. Feasibility gate (before anything is productised)

- [ ] 0.1 On the maintainer's Fly account, hand-make one throwaway sprite: install Docker from Docker's apt repository and register `dockerd` as a sprite Service; verify `docker info` succeeds after a warm wake and after a forced cold wake (`sprite sessions kill` everything, wait past the idle window, `sprite exec -- docker info`)
- [ ] 0.2 In that sprite, install the devcontainers CLI, clone this repository's `main`, run `devcontainer up --workspace-folder . --config .devcontainer/devcontainer.json` with `GH_TOKEN` and `CLAUDE_CODE_OAUTH_TOKEN` in the exec environment, and verify `post-create.sh` completes (including `bot-autonomy.sh apply` and `verify`) and `gh auth status` succeeds inside the container
- [ ] 0.3 Run `task verify` inside the inner container and record wall-clock, peak memory (`/proc/meminfo` sampled during the `shellcheck` step), and whether anything was throttled or killed; record the result in the research note's verification section
- [ ] 0.4 Apply a deny-by-default DNS policy to the spike sprite through the API, then run the whole unconditional loop inside the inner container under it — `task verify`, the full `task security` gate (Semgrep CE with its rule registry and API, gitleaks, the dependency audit), a push of a throwaway branch, `gh pr create --draft`, `gh pr checks`, and the opted-in Codex and Convex steps — iterating the allowlist until every step passes, and record every host it needed as the allowlist shipped in `taskfiles/sprites.yml` (the design notes' starting points, `semgrep.dev` included, are only where to begin); also verify a probe to an unlisted host fails to resolve under the final list
- [ ] 0.5 Under that same policy, install `openssh-server` inside the inner container configured for public-key auth only, install a throwaway public key, find the container's bridge address, and verify a plain `ssh` through `ProxyCommand sprite proxy -s <name> -W <inner-address>:22` lands in the container with the pinned host key; then verify `herdr --remote <alias>` attaches to a Herdr server started inside it
- [ ] 0.6 Verify the activity invariant has at least one working mechanism: (a) start a 3-minute `sleep` through a detachable `sprite exec --tty`, kill the client, and record whether the sprite stays `running` until the command exits; (b) record whether the Tasks API is reachable from inside the sprite without the org token (via `sprite-env` or otherwise) and, if so, hold a Task from an in-sprite Service through the same test; the item passes if either (a) or (b) keeps the sprite active for the command's lifetime, and records which one the helper will use
- [ ] 0.7 Decide: if 0.1–0.6 pass, proceed; if any fails, record the failure in the research note, choose the documented fallback (Fly Machines after slimming the image under 8 GB, Northflank, or Ona), and re-plan this change before touching sections 1–5

## 1. Copier answer and template gating

- [ ] 1.1 Add the `use_fly_sprites` question to `copier.yml` (bool, default no, `when: "[[ devcontainer ]]"`, help text naming the Fly.io account requirement, usage-based pricing including storage while asleep, trial-only free allowance, the private-repository caveat, and the public-base-image-only limit) and verify `task test:copier-validators` and `task test:template:all` pass with the option off and on
- [ ] 1.2 Add the `use_fly_sprites` row to `docs/copier-options.md` and verify `task test:registry-docs`/`lint:markdown` pass
- [ ] 1.3 Gate the new files under `template/` on the answer (jinja-named paths for `taskfiles/sprites.yml`, `scripts/sprite-lane.sh`, `scripts/test-sprite-lane.sh`, `docs/guides/sprites.md`) and every documentation section that describes the option (jinja blocks in the Herdr, devcontainers, and security doc twins), and verify a `--defaults` render contains none of the files, no `sprite:*` task, and no reference to the `sprite` CLI or a Fly.io account anywhere in the rendered tasks, scripts, configuration, or docs (a whole-render grep in `scripts/test-template.sh`), while a `use_fly_sprites: yes` render contains all of them (`task test:template:all`, plus a new profile or an assertion in `scripts/test-template.sh`)
- [ ] 1.4 Record `use_fly_sprites: true` in `.dogfood-answers.yml`, add the root twins, and verify `task test:dogfood-parity` and `task test:dogfood-structure` are green; add any intentional divergence to `scripts/audit-dogfood.sh` with a reason

## 2. Lane helper

- [ ] 2.1 Write `scripts/sprite-lane.sh` with subcommands `pool init|list`, `lane new|exec|attach|harvest|extend|rm`, and `audit [--reclaim]`, a startup prerequisite check that refuses naming the install hint when `flock` (util-linux; `brew install flock` on macOS) or the `sprite` CLI is missing before any platform call, each subcommand shelling to the `sprite` CLI and the devcontainers CLI only (no hand-rolled API calls beyond `sprite api` for the network policy, labels, and any Tasks hold), and verify `task lint:shell` passes (shellcheck `--severity=error`, `shfmt -d`, bash 3.2 portable)
- [ ] 2.2 Implement `pool init`: install Docker and the devcontainers CLI in the sprite, register `dockerd` as a sprite Service, clone the default branch onto the outer host (bot PAT via git's credential helper from the exec environment, never on disk), bring the bot profile up once and tear the containers down, remove the checkout and its env-file, record the golden stamp (image pin, hash of `scripts/sprite-lane.sh` and of the in-sprite supervisor it installs, pinned devcontainers CLI version) in a sprite label, take the golden checkpoint, and refuse beyond the configured pool ceiling; verify with the offline test that the checkout removal precedes the checkpoint, that no file write carries the PAT, that no SSH server is installed on the outer sprite, and that the ceiling refusal fires
- [ ] 2.3 Implement `lane new` in the specified order — refuse a branch absent from the remote or a local branch ahead of the fetched remote head, refuse a stale golden stamp (any component changed), claim a pool sprite under the orchestrator-side per-sprite `flock` (lease written to labels with the pool sprite's platform name and restore state `restoring`; skip expired leases), restore the golden checkpoint and poll until the platform reports it complete (fail non-zero on timeout, leave the lease in `restoring`, print the resume command; a re-run for the same lane resumes at the wait), mark the lease `complete`, set the DNS allowlist policy, clone the remote branch head onto the outer host at `/workspaces/<repo>` (PAT via the credential helper from the exec environment), run `devcontainer up` on that workspace folder with exactly the lane allow-list in the command's environment (`GH_TOKEN`, `CLAUDE_CODE_OAUTH_TOKEN`, opted-in provider keys — never `FOREMAN_AGENT_GH_TOKEN` or `AGENT_DECK_TELEGRAM_KEY`, never the profile's whole `initializeCommand` list) plus the lane marker `HARMON_LANE=1` (forwarded into the container by the `devcontainer.json` twins' `containerEnv`), so `init-env.sh` composes the env-file and post-create runs `bot-autonomy.sh apply`/`verify` authenticated without a conductor or sibling clones, copy the Codex login into the inner container's Codex state (when `use_codex_review` is on), add `archive.ubuntu.com` and `security.ubuntu.com` to the policy, install the in-container SSH server (key-only) with `apt-get update` restricted to the Ubuntu sources, restore the steady-state policy on every exit path of that step (a trap covering failure and interruption), write the lane-local TTL state into the sprite, generate the per-lane keypair, install the public key, record the host key, and write the operator's SSH alias with `ProxyCommand sprite proxy -s <pool-sprite> -W <inner-address>:22` from the leased pool sprite's platform name — and verify the offline test asserts this exact order and that a `verify` failure exits non-zero with the harness named
- [ ] 2.4 Implement the lane lock and the reconcile step every entry runs first: take the orchestrator-side per-sprite `flock` (refusing immediately with `retiring <lane>` if an ending operation holds it), refuse unless the platform reports the lane's restore terminal and successful, check TTL and lease ownership, create the platform session and record its identifier as a cache, release the lock, then reconcile (discarding cached session records the platform no longer lists; `devcontainer up` if the inner container is not running, which reruns `post-start.sh` and its `verify`; restart the in-container SSH server and the in-container Herdr server; on a new container identity reinstall sshd, regenerate `authorized_keys`, re-pin the host key over `sprite exec`, refresh the alias; otherwise refresh the SSH alias address), then `lane exec` (a detachable `sprite exec --tty` into the inner container that runs the given command), `lane attach` (`sprite sessions attach` for the lane's session, or — when the session is gone after a cold pause — a new detachable shell session in the container plus a message naming the lost session and the resume options), and `lane harvest` (which reconciles the same way before copying the report out); verify with the stubbed CLI that all three create and record the session under the lock and record nothing before the session exists, reconcile first (including the sshd restart and the recreation re-provisioning), target the inner container, never the sprite's own shell, and that attach on a session-less lane opens a fresh session rather than failing
- [ ] 2.5 Implement the activity invariant and the command bound: every lane command runs as a detachable in-sprite exec session under an in-sprite supervisor that applies the per-command duration bound (default 2 h), reads the TTL and grace from the lane-local state file inside the sprite (never from its own launch arguments), registers and releases any Tasks-API hold, and stops the command with its output intact at the bound; no heartbeat, hold, or timer is owned by a process on the orchestrator's machine; verify the offline test asserts no orchestrator-side hold or timer and that killing the orchestrator-side helper records no release call
- [ ] 2.6 Implement the TTL and lease renewal: `lane new` writes the TTL into the sprite's lane-local state and mirrors it in the lease, `lane extend` takes the lane lock, validates ownership, refuses unless the restore is terminal and successful, writes the new TTL into the in-sprite state first and then updates the lease's TTL and expiry (refusing with `retiring <lane>` while an ending operation holds the lock), every authenticated activity (`lane exec`, `lane attach`, `lane harvest`) and `lane extend` renew the lease by the configured lease window with expiry never earlier than the TTL, and past TTL expiry `lane exec` refuses new commands naming `lane extend`, a running command gets the grace period (default 30 min) and is then stopped by the in-sprite supervisor with the checkout, container, and volumes intact; verify with the offline test, including a long-lived valid lane that is never reported reclaimable and an extend during an active command whose supervisor observes the new TTL
- [ ] 2.7 Implement `lane harvest` (reconcile, then copy the report file to the orchestrator's scratch directory and print the path) and `lane rm` holding the lane lock from its first check until the restore completes and the lease is released: evaluate activity from the platform's session list only (refuse while it reports a live session; discard a stale cached record) and the cleanliness gate over every git checkout under `/workspaces` — porcelain status non-empty, or the branch has commits not on its upstream (`git rev-list --count @{upstream}..HEAD` non-zero), no upstream, or no remote counterpart — refuse without `--force` releasing the lock with nothing changed, otherwise stop the container, scrub the env-file (the one file carrying the PAT), the Codex state, the container's `authorized_keys`, and any process environment, remove the per-lane keypair and SSH alias on the orchestrator, restore the golden checkpoint and wait for completion, release the lease, release the lock, return to pool or `--destroy`; add `lane harvest --bundle` (a git bundle of the lane branch for the operator handoff of workflow-touching changes) and make the lane's push step refuse a commit touching `.github/workflows/**` naming that handoff (the hook-level guard is task 3.5); write retirement once and have `audit --reclaim` call it; verify the lock span, the activity refusal, the sibling-checkout refusal, the cleanliness refusal, the scrub order, the lease release, and the workflow refusal with the offline test
- [ ] 2.8 Implement `audit`: one row per pool sprite and lane (name, state, age, TTL, lease owner, issue, golden pin) from sprite labels, expired lanes and expired leases flagged, non-zero exit on any expired lane; `audit --reclaim` is `lane rm` invoked on an expired lease after the confirmation prompt — the same lane lock held for the same span, activity evaluation, cleanliness gate, and `--force` — never a second procedure; verify with the stubbed CLI's canned `sprite list` and `sprite sessions list` output, including the idle-but-unclean refusal without `--force`
- [ ] 2.9 Write `scripts/test-sprite-lane.sh` with a stubbed `sprite` and `devcontainer` on `PATH` that record every invocation, asserting the ordering rule (claim → restore complete → policy → clone → `devcontainer up` with credentials → Codex login → SSH keys → agents), every pre-restore refusal (absent or ahead branch, stale golden stamp with the image pin, the helper hash, and the CLI pin each changed alone, full pool, contended lease, expired lease, retirement or reclaim over a live platform session, reclaim over an idle-but-unclean checkout without `--force`, non-clean retirement including a clean tree ahead of its upstream and a dirty sibling checkout under `/workspaces`, expired TTL, a workflow-touching push) recording no restore call, the lane-lock interleavings driven deterministically against a stubbed `flock` (exec and retirement contending with exactly one holder; a refused retirement releasing the lock with nothing changed and the next exec running; `lane extend` refused while reclaim holds the lock; a crash at each mid-startup point leaving no session record that the platform does not have), a stale cached session record never blocking retirement, entry during a pending restore refused, a missing `flock` refused before any `sprite` call, lease renewal on every authenticated activity and `lane extend` with expiry never earlier than the TTL and the in-sprite TTL write preceding the lease write, the two policy writes bracketing the SSH install plus the restoring write on the failure and interruption paths, the `HARMON_LANE` marker present in every `devcontainer up` environment and forwarded by the rendered `devcontainer.json` twins, the `guard:lane-workflow-push` hook refusing a real `git push` against a stubbed remote inside a lane and staying inert outside one, SSH re-provisioning on a container identity change, the allow-list exactness case (the environment handed to `sprite exec` and `devcontainer up` is exactly `GH_TOKEN`, `CLAUDE_CODE_OAUTH_TOKEN`, and opted-in provider keys, with `FOREMAN_AGENT_GH_TOKEN` and `AGENT_DECK_TELEGRAM_KEY` present on the orchestrator and absent from it), the reconcile-first rule for exec, attach, and harvest alike, the incomplete-restore case recording exactly one restore call and nothing after it, the no-orchestrator-side-hold-or-timer rule, that the Sprites token (env var, token file, keyring path) appears in no written file, and that the bot PAT appears in no file other than the checkout's `.devcontainer/devcontainer.env` (mode `0600`, scrubbed at retirement); wire it as `task test:sprite-lane` into `task verify` (root) and verify `task verify` is green

## 3. Taskfile and configuration

- [ ] 3.1 Add `taskfiles/sprites.yml` defining the **unprefixed** children `pool:init`, `pool:list`, `lane:new`, `lane:exec`, `lane:attach`, `lane:harvest`, `lane:extend`, `lane:rm`, and `audit`, each a trivial `cmds:` call into `scripts/sprite-lane.sh`, and include it from `Taskfile.yml` as `sprite: taskfiles/sprites.yml` exactly as `taskfiles/foreman.yml` is included under `foreman:`, so the surface is `task sprite:lane:new` and never `sprite:sprite:…`; verify `task test:tasks` passes and `task --list-all` shows the `sprite:*` tasks once each
- [ ] 3.2 Add the pool ceiling, lane TTL default, TTL grace period, per-command bound, lease window (renewal amount; expiry never earlier than the TTL), restore-wait timeout, and the egress allowlist (the list recorded by task 0.4, never hand-edited) as variables in `taskfiles/sprites.yml` (with the plan-limit rationale as comments) and verify the helper reads them and the offline test exercises the ceiling and the timeout
- [ ] 3.3 Add the `HARMON_LANE` guard to `.devcontainer/scripts/bootstrap-related-repos.sh`, `fetch-related-repos.sh`, `post-create-conductor.sh`, and the conductor-start and Telegram-bridge blocks of `post-start-common.sh` (root and template twins, verbatim), each a skip-when-set at the top of the block that treats empty as unset and changes nothing when unset, and in the same task forward the marker into the container by adding `"HARMON_LANE": "${localEnv:HARMON_LANE}"` to `containerEnv` in the root `.devcontainer/devcontainer.json` and its jinja twin `template/[% if devcontainer %].devcontainer[% endif %]/devcontainer.json.jinja`; verify `task test:dogfood-parity`, `task test:dogfood-structure`, `task lint:shell`, `task test:devcontainer:permissions`, a stubbed run of each script with the marker set records no clone, fetch, conductor, or bridge call, and a rendered bot container with `HARMON_LANE=1` in the host environment shows it in `postCreateCommand`'s environment
- [ ] 3.5 Add the Taskfile-backed `guard:lane-workflow-push` (root and template twins): when `HARMON_LANE` is set and the pushed range touches `.github/workflows/**`, exit non-zero naming the bundle handoff; inert when the marker is unset; wire it into the lefthook `pre-push` hook beside `secrets`; verify `task test:tasks`, `task lint:shell`, and an offline test that runs the hook against a stubbed remote inside a marked checkout (refuses before any network call) and an unmarked one (passes)
- [ ] 3.4 Update the `runner` comment in `.foreman.toml` (root and template twin) to state what `"sprite"` will mean and that dispatch stays refused until Foreman ships the runner; verify `task test:dogfood-parity` still passes and the value is unchanged

## 4. Documentation

- [ ] 4.1 Write `docs/guides/sprites.md` (root and template twin): prerequisites (Fly account, `sprite` CLI, `sprite org auth`, `flock` — util-linux on Linux, `brew install flock` on macOS), pool init, the lane loop from Herdr with a worked brief/sentinel/report example, takeover with `herdr --remote` and the in-container SSH alias, the one-orchestrator-per-pool limit, cost controls (activity invariant, TTL and `lane extend`, audit and reclaim), and the "what Sprites cannot do and the alternative" table; verify `task lint:markdown` and `task test:dogfood-structure`
- [ ] 4.2 Add a "Remote lanes" section to `docs/guides/herdr.md` (and twin) pointing at the sprites guide and stating the pane-process-is-the-session topology and the `unknown`-detection fallback; verify `task test:dogfood-structure`
- [ ] 4.3 Add the sprite credential boundary to `docs/architecture/security.md` (what enters a lane, what never does, the Sprites org token rule) and a pointer from `docs/guides/devcontainers.md`; verify `task lint:markdown`
- [ ] 4.4 Add the dispatch selector (`--env sprite`) to the dev-loop dispatch text this repository owns and verify `task audit:agent-instructions`/`test:agent-instructions-size` stay green

## 5. Follow-ups filed and verification

- [ ] 5.1 File the follow-up issues named in the research note's recommendation — harmon-devkit (herdr skill and dispatch-recipe text; `standardize-repo` catalog entry), ponderousdev/foreman (runner naming and D5/D6 reconciliation; the 8 GB rootfs limit against its own image), harmon-init (shorter-lived lane credentials; a scoped read-credential path for a private devcontainer base registry, unsupported in v1; baking `openssh-server` into the shared image so the two Ubuntu package hosts leave the lane policy; a shared-pool lock primitive for more than one orchestrator, unsupported in v1), harmon-infra (the Coder template README note that per-lane Coder workspaces are the fallback and that no GHCR read token is needed for the public shared image) — post the Fly.io feature request (sprite-scoped tokens; fork-from-sprite) on community.fly.io and record its URL in the research note, and verify each issue is linked from the implementation PR body with `Refs`
- [ ] 5.2 Run the first real lane end to end on the maintainer's Fly account (the `[HUMAN]` criterion on harmon-init#1120) through the shipped tooling and record wall-clock, cost, Herdr detection result, memory behaviour under `task verify`, and the literal egress hostnames in the research note; verify the note's "Verification status" section is updated and the open questions in design.md are answered or re-deferred
- [ ] 5.3 In that same run, execute and record the platform-dependent security scenarios the spec names: after `lane rm`, no credential remains in the sprite's filesystem or environment; the golden checkpoint holds none; a restore removes the previous lane's state; the policy file is read-only inside and a host outside the allowlist fails to resolve; a cold-woken lane reconciles with `verify` passing; a detached session survives the orchestrator's pane; verify each is marked pass/fail in the research note and any failure blocks marking this change complete
